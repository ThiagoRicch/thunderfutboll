import './Button.css'

const Button = (props) => {
  return (
    <div className='buttonContainer'>
        <button className = "buttonStyle">
            Criar Card
        </button>
    </div>
    )
};

export default Button;